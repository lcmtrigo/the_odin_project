<div>
	<h4>Comment by <%= comment.author_name %></h4>
	<%= "Article created_at: #{comment.article.created_at} Comment created_at: #{comment.created_at}" %>
	<p class="comment"><%= comment.body %></p>
</div>